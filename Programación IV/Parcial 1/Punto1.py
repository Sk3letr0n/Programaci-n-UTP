'''
1. Haciendo uso de los comentarios defina los siguientes conceptos:

Clase: 
En programación orientada a objetos, una clase define las características de un objeto. 
Una clase es una plantilla que define propiedades y métodos, es un modelo abstracto a partir del cual se
pueden crear instancias más específicas.

Objeto: Un objeto es una instancia concreta de una clase que tiene atributos y métodos específicos, y representa un 
elemento del mundo real o una abstracción en el software.

Self: Self ofrece un modo de cálculo para hacer referencia al contenido del objeto con el que está asociado sin
tener que hacer referencia explícitamente al objeto. Con Self, puede crear una única fórmula de cálculo que se 
puede aplicar a los distintos objetos.

Constructor: Un constructor es un método especial en una clase que se llama automáticamente cuando se crea una nueva
instancia del objeto. Se utiliza para inicializar los atributos y configurar el objeto.

Programación orientada a objetos:La programación orientada a objetos (POO) es un paradigma de programación que se
basa en la creación de clases y objetos, lo que permite organizar el código de manera modular y reutilizable 
al modelar conceptos del mundo real como objetos.

Metodo: Un método es un bloque de código que contiene una serie de instrucciones. Un programa hace que se 
ejecuten las instrucciones al llamar al método y especificando los argumentos de método necesarios.

Instancia: Las instancias son la materialización de una clase, es decir, son objetos concretos que tienen 
su propio estado (los valores de sus atributos) y su propio comportamiento (los métodos que pueden ejecutar).

'''
# Hecho por: Steven Grisales López (1034289634) y Luis Felipe Garzón Bonilla (1089931169)